# Copiar para .env. Só estas duas — o front nunca vê chave de servidor.
#
# A anon key é pública por natureza: quem protege o dado é a RLS, não ela.
# Chave de service_role NUNCA entra aqui: tudo com prefixo VITE_ vai para
# o navegador.

VITE_SUPABASE_URL=https://labkfsakestwjxmcmirj.supabase.co
VITE_SUPABASE_ANON_KEY=
